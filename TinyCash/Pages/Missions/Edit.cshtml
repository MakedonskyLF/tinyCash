@page
@model TinyCash.Pages.Missions.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Mission</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Mission.ID" />
            <div class="form-group">
                <label asp-for="Mission.Name" class="control-label"></label>
                <input asp-for="Mission.Name" class="form-control" />
                <span asp-validation-for="Mission.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Mission.Description" class="control-label"></label>
                <input asp-for="Mission.Description" class="form-control" />
                <span asp-validation-for="Mission.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Mission.Priority" class="control-label"></label>
                <input asp-for="Mission.Priority" class="form-control" />
                <span asp-validation-for="Mission.Priority" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Mission.ExpectedCost" class="control-label"></label>
                <input asp-for="Mission.ExpectedCost" class="form-control" />
                <span asp-validation-for="Mission.ExpectedCost" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
